<!--div class="sidebar well_sidebar">
{{ site.description }}
</div-->

<div class="sidebar well_sidebar">
	<h1> Recent Post </h1>
    <ul>
</tr>
<div>All------------{{site.categories | size}}</div>
	{% for category in site.categories %}
	<li>{{category | first}}------------{{category | last | size}}</li>
	{% endfor %}

	{% for post in site.posts: %}   
	<!--li><a href="{{ site.baseurl}}{{ post.url }}">{{ post.title }}-{{ post.category }}</a></li-->
        {% endfor %}
    </ul>
</div>

<!--div class="sidebar well_sidebar">
{% include links-list.html %}
</div-->
